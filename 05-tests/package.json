{
  "name": "sales-crm-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E suite for the Sales CRM (backend :8000 + frontend :3000)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2"
  }
}
